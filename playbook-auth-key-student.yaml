---
- hosts: all
  become: true
  tasks:
    - name: Set authorized key taken from file
      ansible.posix.authorized_key:
        user: student
        state: present
        key: "{{ lookup('file', 'ansible.pub') }}"
...